apiVersion: v1
kind: Service
metadata:
  name: readeck
  namespace: media
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: readeck
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort | int }}
