apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readeck-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn-2replicas
